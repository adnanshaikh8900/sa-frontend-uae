apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-simplevat-uae.fullname" . }}-test-connection"
  labels:
    {{- include "helm-simplevat-uae.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-simplevat-uae.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
